{
  "name": "temporal-graph-vis",
  "version": "1.0.0",
  "description": "Temporal graph visualization using Neo4j, Flask, and Sigma.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild frontend/script.js --bundle --outfile=frontend/dist/bundle.js --minify --sourcemap",
    "watch": "esbuild frontend/script.js --bundle --outfile=frontend/dist/bundle.js --sourcemap --watch"
  },
  "keywords": [
    "graph",
    "visualization",
    "temporal",
    "neo4j",
    "flask",
    "sigma.js"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.11.13",
    "graphology": "^0.26.0",
    "graphology-layout-forceatlas2": "^0.10.1",
    "sigma": "^3.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}
